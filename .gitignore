# Secrets — never commit a real Doable key
.env
.env.local

# OS / editor
.DS_Store
*.swp

# Node (if you add tooling later)
node_modules/

# Agent client local state
.claude/settings.local.json

# Real MCP config (copied from .mcp.json.example, holds your Doable key)
.mcp.json
